import { createAction } from '@reduxjs/toolkit'

// export const updateActionBtnType = createAction('UPDATE_ACTION_BUTTON_TYPE')

export const updateSearchQuery = createAction('UPDATE_SEARCH_QUERY')

export const addClaim = createAction('ADD_CLAIM')

export const delClaim = createAction('DEL_CLAIM')

export const updateClaims = createAction('UPDATE_CLAIMS')
